# Report.tsx 测试指南

## 测试前准备

1. **确保开发服务器正在运行**
   ```bash
   npm run dev
   ```

2. **检查环境变量**
   - 确保 `.env` 文件存在且包含：
     - `VITE_API_KEY`
     - `VITE_API_BASE_URL`
     - `VITE_AI_MODEL`

## 测试步骤

### 1. 基础流程测试

#### 步骤 1.1: 访问首页
- 打开浏览器访问 `http://localhost:5173`
- ✅ 应该能看到首页

#### 步骤 1.2: 创建评估
- 点击"开始测评"或类似按钮
- 填写用户信息（姓名、年龄、性别）
- ✅ 应该能进入答题页面

#### 步骤 1.3: 完成答题
- 回答所有 65 道题目
- ✅ 每道题都能正常选择并保存

#### 步骤 1.4: AI 对话（可选）
- 完成答题后进入 AI 对话页面
- 发送几条消息测试 AI 回复
- ✅ AI 应该能正常回复

#### 步骤 1.5: 生成报告
- 点击"生成报告"按钮
- ✅ 应该跳转到 `/report?id=1`（或相应的 ID）

### 2. 报告页面测试

#### 步骤 2.1: Loading 状态
- 进入报告页面后
- ✅ 应该看到 Loading 动画
- ✅ 应该看到进度提示文字（"正在分析五维人格..."等）
- ✅ Loading 动画应该持续旋转

#### 步骤 2.2: 报告生成成功
- 等待 10-20 秒（AI 生成报告需要时间）
- ✅ 应该看到完整的报告页面
- ✅ 不应该出现任何错误

#### 步骤 2.3: 报告内容检查
- ✅ 顶部应该显示用户头像和称号
- ✅ 应该能看到雷达图
- ✅ 应该能看到 6 个 Tab（概览、深度、建议、规划、家长、附录）

### 3. Tab 切换测试

#### 步骤 3.1: 概览 Tab
- 点击"概览" Tab
- ✅ 应该显示用户头像、称号、金句
- ✅ 应该显示雷达图（五维能力）
- ✅ 应该显示形象比喻卡片
- ✅ 应该显示核心特质（优势/潜力/关注）

#### 步骤 3.2: 深度 Tab
- 点击"深度" Tab
- ✅ 应该显示大五人格分析（5 个维度）
- ✅ 每个维度应该有进度条和得分
- ✅ 每个维度应该有"对话中的证据"（紫色引用块）
- ✅ 应该显示霍兰德兴趣类型
- ✅ 应该显示学习风格

#### 步骤 3.3: 建议 Tab
- 点击"建议" Tab
- ✅ 应该显示多个行动建议卡片
- ✅ 每个卡片应该有：标题、类型标签、时间要求、科学原理、执行步骤、预期效果

#### 步骤 3.4: 规划 Tab
- 点击"规划" Tab
- ✅ 应该显示周计划（至少 4 周）
- ✅ 应该显示每日 SOP（早中晚）

#### 步骤 3.5: 家长 Tab
- 点击"家长" Tab
- ✅ 应该显示话术红黑榜（左右对比）
- ✅ 应该显示每日检查清单

#### 步骤 3.6: 附录 Tab
- 点击"附录" Tab
- ✅ 应该显示推荐书籍、电影、游戏（如果有）

### 4. 错误处理测试

#### 步骤 4.1: 无评估 ID
- 直接访问 `http://localhost:5173/report`（不带 id 参数）
- ✅ 应该显示错误提示："未找到评估ID"

#### 步骤 4.2: 无效评估 ID
- 访问 `http://localhost:5173/report?id=99999`
- ✅ 应该显示错误提示："评估记录不存在" 或 "报告生成失败"

#### 步骤 4.3: API 错误
- 如果 API 配置错误或网络问题
- ✅ 应该显示友好的错误提示
- ✅ 应该显示"返回首页"按钮

### 5. 边界情况测试

#### 步骤 5.1: 空数据测试
- 如果报告数据不完整
- ✅ 应该显示"报告数据不完整，请重试"

#### 步骤 5.2: 浏览器控制台检查
- 打开浏览器开发者工具（F12）
- 切换到 Console 标签
- ✅ 不应该有红色错误信息
- ✅ 如果有警告，应该是非致命的

### 6. 性能测试

#### 步骤 6.1: 加载速度
- 报告生成应该在 10-20 秒内完成
- ✅ 不应该超过 30 秒

#### 步骤 6.2: 页面响应
- 切换 Tab 时
- ✅ 应该立即响应，无延迟

## 常见问题排查

### 问题 1: 页面一直显示 Loading
**可能原因：**
- API 调用失败
- 网络问题
- API 配置错误

**解决方法：**
1. 检查浏览器控制台的错误信息
2. 检查 `.env` 文件中的 API 配置
3. 检查网络连接

### 问题 2: 报告数据不完整
**可能原因：**
- AI 返回的 JSON 格式不正确
- 数据解析失败

**解决方法：**
1. 检查浏览器控制台的错误信息
2. 检查 `reportService.ts` 中的 JSON 解析逻辑

### 问题 3: 页面崩溃（白屏）
**可能原因：**
- 渲染错误
- 数据解构失败

**解决方法：**
1. 检查浏览器控制台的错误信息
2. 检查是否有未捕获的异常
3. 查看错误堆栈信息

## 测试检查清单

- [ ] 首页正常加载
- [ ] 创建评估流程正常
- [ ] 答题流程正常
- [ ] AI 对话正常（可选）
- [ ] 报告生成按钮正常
- [ ] Loading 状态正常显示
- [ ] 报告页面正常渲染
- [ ] 所有 6 个 Tab 都能正常切换
- [ ] 每个 Tab 的内容都正常显示
- [ ] 错误处理正常（无 ID、无效 ID）
- [ ] 浏览器控制台无错误
- [ ] 页面响应速度正常

## 成功标准

✅ **所有测试项都通过**
✅ **浏览器控制台无错误**
✅ **页面渲染流畅**
✅ **所有功能正常工作**

---

**测试完成后，请告诉我结果！** 🚀
